export const SYSTEM_PROMPT = `Ты — «Resume Designer», ИИ-помощник для создания и улучшения резюме.

Правила:
- Отвечай на русском языке, профессионально и дружелюбно.
- Структура резюме — строго по стандарту HeadHunter (hh.ru): шапка (личные данные, город, гражданство, желаемая должность, зарплата, суммарный опыт) → опыт работы по местам (период, длительность, должность, компания, обязанности и достижения) → образование → обо мне → иностранные языки → водительские права.
- Опыт оформляй по каждому месту работы отдельно, от нового к старому; достижения — с метриками (цифры, %, суммы, KPI).
- При анализе проверяй каждый блок иерархии HH и указывай, что отсутствует или слабо заполнено.
- При создании выдавай черновик в той же иерархии, как готовое резюме на hh.ru.
- Если данных недостаточно — задай до 3 уточняющих вопросов по недостающим блокам (не выдумывай факты).
- Не выдумывай факты о пользователе; опирайся только на предоставленный текст.
- Не используй мат, оскорбления, грубые формулировки и политические высказывания.
- Если во входных данных есть такое — не составляй резюме и не продолжай диалог.

Формат ответа: связный текст с подзаголовками блоков + при необходимости маркированные списки рекомендаций или достижений.`;

export const HH_STRUCTURE = `Эталонная иерархия резюме HeadHunter (сверху вниз, как на hh.ru):

1. Фото соискателя (опционально)
   — превью/аватар; не выдумывать, если не указано.

2. Личная информация (шапка)
   — возраст и дата рождения (например: «39 лет (родился 01 января 1987)»);
   — уровень образования в шапке (например: «высшее образование»);
   — семейное состояние и дети (например: «состоит в браке, есть дети»).

3. Город проживания
   — город, где соискатель ищет работу / готов работать.

4. Гражданство
   — страна гражданства.

5. Желаемая должность
   — название позиции, на которую ориентировано резюме.

6. Зарплата
   — ожидаемый уровень дохода (сумма и валюта).

7. Общий опыт работы
   — суммарная длительность (например: «Опыт работы 18 лет и 5 месяцев»).

8. Опыт работы (каждое место — отдельный блок, в порядке от нового к старому)
   Для каждой позиции:
   — период: «Январь 2011 – работает сейчас» или «Апрель 2008 – декабрь 2010»;
   — длительность на позиции: «15 лет и 8 месяцев»;
   — должность;
   — компания: название, тип/размер («небольшая компания», «крупная»), город;
   — «Обязанности и достижения»: маркированный список с конкретикой, метриками, KPI, клиентами.

9. Образование
   — уровень (высшее / среднее и т.д.);
   — форма обучения (дневная/очная, заочная);
   — год окончания;
   — учебное заведение;
   — факультет;
   — специальность.

10. Обо мне
    — дополнительные сведения, сильные стороны, компетенции;
    — маркированный список ключевых навыков и опыта.

11. Иностранные языки
    — язык и уровень (например: «Английский — разговорный»).

12. Водительские права
    — категории (например: «B — легковые авто»).

Правила иерархии:
— Блоки 2–6 формируют «шапку» резюме перед опытом.
— Опыт (8) всегда структурирован по местам работы, не смешивать компании в один абзац.
— Достижения в опыте — через результаты: цифры, %, суммы, количество клиентов/сделок.
— Если блок отсутствует в тексте — явно отметить «не указано» при анализе; при создании — задать уточняющий вопрос.`;

const ANALYSIS_TEMPLATE = `Проанализируй резюме пользователя по стандарту HeadHunter.

{{ resume_structure }}

Целевая должность (если указана): {{ target_role }}

Текст резюме:
---
{{ resume_text }}
---

Задача:
1. Разложи текст на блоки по иерархии HH (п. 1–12 выше). Для каждого блока укажи: «есть / неполно / отсутствует» и краткий комментарий.
2. Проверь порядок: шапка (личные данные, город, гражданство, должность, зарплата, общий опыт) → опыт по местам → образование → обо мне → языки → права.
3. В блоке «Опыт работы»: отдельно оцени каждое место — период, длительность, компания, обязанности и достижения с метриками.
4. Оцени конкретность достижений (цифры, KPI, влияние на бизнес).
5. Соответствие целевой должности и рынку.

Формат ответа:
— **Краткий вердикт** (2–3 предложения).
— **Разбор по блокам HH** — подзаголовки по номерам иерархии (2–12), что хорошо и что улучшить.
— **3–5 приоритетных рекомендаций** (маркированный список).
— Если не хватает данных для сильного резюме — **2–3 уточняющих вопроса** (по недостающим блокам или метрикам в опыте).`;

const CREATION_TEMPLATE = `Помоги пользователю составить резюме в формате HeadHunter.

{{ resume_structure }}

Сообщение пользователя:
---
{{ user_message }}
---

{% if resume_context %}
Уже известный контекст (ответы анкеты / черновик):
{{ resume_context }}
{% endif %}

Задача:
1. Определи этап: сбор данных / черновик / доработка.
2. Если данных недостаточно для блоков иерархии HH — задай 2–3 целевых вопроса (по недостающим блокам: личные данные, опыт с метриками, образование, обо мне, языки).
3. Если данных достаточно — выдай **черновик резюме**, строго соблюдая иерархию HH (п. 1–12): шапка → опыт по местам (от нового к старому) → образование → обо мне → языки → права.
4. В каждом месте работы: период, длительность, должность, компания (тип, город), список «Обязанности и достижения» с метриками.
5. Не выдумывай факты — только из контекста пользователя; пропущенные поля помечай «не указано» или проси уточнить.

Формат черновика: читаемый текст с подзаголовками блоков, как на hh.ru (без HTML).`;

function render(template, vars) {
  let result = template;
  const hasContext = Boolean(vars.resume_context);
  result = result.replace(
    /\{% if resume_context %\}([\s\S]*?)\{% endif %\}/g,
    hasContext ? "$1" : "",
  );
  for (const [key, value] of Object.entries(vars)) {
    result = result.replaceAll(`{{ ${key} }}`, value ?? "");
  }
  return result.trim();
}

export function buildUserPrompt(intent, { userMessage, resumeText, targetRole, resumeContext }) {
  if (intent === "analyze_resume") {
    return render(ANALYSIS_TEMPLATE, {
      resume_structure: HH_STRUCTURE,
      target_role: targetRole || "не указана",
      resume_text: resumeText || userMessage,
    });
  }
  return render(CREATION_TEMPLATE, {
    resume_structure: HH_STRUCTURE,
    user_message: userMessage,
    resume_context: resumeContext || "",
  });
}
